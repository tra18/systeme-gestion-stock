[phases.setup]
nixPkgs = ["python3", "gcc", "freetype", "libffi", "zlib"]

[phases.install]
cmds = [
  "python -m venv /opt/venv",
  ". /opt/venv/bin/activate",
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn main_simple:app --host 0.0.0.0 --port $PORT"
